<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusConnect">

        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity android:name=".CanteenActivity" />

        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />

        <activity android:name=".ProfileActivity" />

        <activity android:name=".CreateListingActivity" />

        <activity android:name=".GroupFeedActivity" />

        <activity android:name=".GroupDetailsActivity" />

        <activity android:name=".TaskManagerActivity" />

        <receiver android:name=".receivers.AlertReceiver" />

        <activity android:name=".QuickLinksActivity" />

        <activity android:name=".LinkCategoriesActivity" />

        <activity android:name=".DoubtFeedActivity" />

        <activity android:name=".AskDoubtActivity" />

        <activity android:name=".DoubtDetailsActivity" />

        <activity
            android:name=".LostAndFoundActivity"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize" />
        <activity android:name=".PostLostItemActivity" />

        <activity android:name=".LostFoundDetailsActivity" />

        <activity android:name=".FacultyDirectoryActivity" />

        <activity android:name=".FacultyDetailsActivity" />


    </application>

</manifest>